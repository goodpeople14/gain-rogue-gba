# Gain Rogue GBA 프로젝트 규칙

## 작업 전

- 구현 전에 관련 코드, 자산 메타데이터와 가까운 문서를 먼저 확인한다.
- 편집 전에 현재 브랜치와 기존 변경 사항을 확인한다.
- 사용자의 변경을 보존하고 요청 범위 밖의 파일은 수정하지 않는다.
- 기능 변경에 관계없는 대규모 리팩터링을 섞지 않는다.
- 기존 구조나 공통 구성 요소가 요청한 책임을 이미 담당하면 재사용한다.
- 작업 지시서나 설계 제안에서는 `실제 Repository에서 확인한 사실`, `확인된 사실을 바탕으로 한 설계 판단`, `아직 확인하지 못해 구현 전에 검증이 필요한 사항`을 구분한다. 확인하지 않은 내용을 현재 구조의 사실처럼 단정하지 않는다.

## 멀티 에이전트 작업 방식

- 주 에이전트는 총지휘자 역할을 맡아 목표 확인, 작업 분해, 담당 지정, 결과 통합과 최종 판단을 수행한다.
- 조사·구현·검증·리뷰처럼 서로 독립적으로 나눌 수 있고 효과가 분명한 작업에만 하위 에이전트를 사용한다. 단순한 한 파일 수정, 짧은 질문 또는 밀접하게 연결된 작업은 주 에이전트가 직접 수행한다.
- 두 개 이상의 독립적인 조사나 검증이 필요하면 하위 에이전트 사용을 우선 검토하되, 하위 에이전트 사용 자체를 목표로 삼지 않는다. 작업 분담의 효과가 소통과 통합 비용보다 클 때만 위임한다.
- 하위 에이전트에게 작업 범위, 기대 결과와 담당 파일을 명확하게 지정한다. 같은 파일을 여러 에이전트가 동시에 수정하지 않도록 담당 범위를 분리한다.
- 하위 에이전트는 조사 결과, 변경 파일, 실행한 검증, 남은 위험과 차단 사항을 주 에이전트에게 보고한다.
- 주 에이전트는 위임한 결과를 직접 검토하고 통합하며, 하위 에이전트의 결론을 검증 없이 적용하지 않는다. 충돌 해결, 최종 검증과 최종 결과를 책임진다.
- 커밋은 주 에이전트만 수행한다. 사용자가 별도로 요청하지 않는 한 하위 에이전트는 커밋, push, PR 생성 또는 병합을 수행하지 않는다.
- 모든 에이전트는 기존 사용자 변경을 보호하고 저장소의 빌드·검증·코딩 규칙을 동일하게 준수한다. 저장소의 기존 규칙이 이 절보다 구체적이면 기존의 구체적인 규칙을 우선한다.

## 프로젝트 구조

- 플레이어 입력을 명령으로 변환하는 책임은 `PlayerController`에 둔다.
- 공통 위치, 방향, 이동속도, 스프라이트와 충돌 몸체는 `Character`에서 관리한다.
- 검사 공격 같은 캐릭터 고유 행동은 구체 캐릭터 클래스에 둔다.
- `GameScene`은 시스템을 조정하되 개별 시스템의 모든 동작을 직접 구현하지 않는다.
- `Hitbox`, `Hurtbox`, `Pushbox`의 구분을 유지한다.
- `include/game/direction.h`의 방향 순서를 보존한다. 스프라이트 프레임 인덱스와 방향별 데이터가 이 순서에 의존한다.
- 게임 객체 모음에는 고정 크기 Butano 컨테이너와 미리 정한 최대 개수를 우선 사용한다. 필요성이 확인되지 않은 동적 할당은 도입하지 않는다.

자산 규격은 `docs/graphics-rules.md`, Sprite palette 정책은 `docs/gba-sprite-palette.md`, 전투 구조는 `docs/combat-rules.md`를 따른다.

## 프로젝트 기억과 문서

- 코드를 현재 동작의 1차 근거로 취급한다. 문서와 코드가 충돌하면 문서를 사실로 가정하지 말고 실제 구현과 변경 이력을 먼저 확인한다.
- `docs/architecture.md`는 현재 구조를 빠르게 찾기 위한 얇은 지도다. 세부 구현을 중복해서 장황하게 기록하지 않는다.
- `docs/decisions/`에는 중요한 설계 결정과 이유를 기록한다. 기존 결정을 바꿀 때 과거 기록을 현재 결론으로 덮어쓰지 않고 새 결정에서 변경 이유를 남긴다.
- 기존 Decision과 충돌하는 구조 변경이 필요하면 AI가 임의로 확정하지 않는다. 충돌하는 기존 결정, 변경이 필요한 이유, 유지안과 변경안을 보고하고 사용자 승인 후 새 Decision으로 변경 이유를 기록한다.
- `docs/journal/`은 작업 일지가 아니다. 생각이 바뀐 과정, 예상과 달랐던 결과, 실패와 발견, 여러 대안의 비교와 선택, 향후 책에 남길 가치가 있는 교훈을 기록한다.
- Git commit, Issue, PR로 충분히 알 수 있는 단순 작업 내역은 저널에 반복 기록하지 않는다.
- 저널은 모든 작업에서 전부 읽지 않는다. 과거 판단의 이유가 필요하거나 기존 결정과 충돌하거나 관련 설계 맥락이 필요할 때 해당 저널만 선택적으로 확인한다.
- 매 작업마다 저널을 만들지 않는다. 중요한 판단 변화, 예상 밖의 발견, 실패에서 얻은 교훈, 의미 있는 대안 선택처럼 기록할 가치가 있는 사건이 있을 때만 AI가 저널 초안을 제안한다.
- 의미 있는 설계 변화나 학습이 있었으면 작업 검증 후 관련 decision/journal 갱신 필요성을 확인한다. 기록할 가치가 없으면 문서를 만들지 않는다.
- AI는 decision/journal 초안을 작성할 수 있지만 중요한 설계 결정의 최종 승인자는 사용자다.

## 자산과 생성 파일

- 루트 `Makefile`에 필요한 소스 및 그래픽 디렉터리가 등록되어 있는지 확인한다.
- `build/`, `.gba`, `.elf`, 에뮬레이터 저장 및 상태 파일을 커밋하지 않는다.
- 생성 도구나 검증 도구가 있는 자산은 원본 도구를 함께 갱신하고 실행한다.
- 요청과 소비 코드를 함께 변경하지 않는 한 기존 자산의 크기, 프레임 순서, 팔레트와 메타데이터를 바꾸지 않는다.

## 검증

- Butano 도구 체인을 사용할 수 있으면 코드 또는 빌드 대상 자산 변경 후 `make -j2`를 실행한다.
- 빌드 설정, 생성 자산 또는 오래된 산출물 문제를 확인할 때는 `make clean` 후 `make -j2`를 실행한다.
- 최종 diff에서 요청한 파일만 변경되었는지 확인한다.
- 기존 실패와 이번 변경으로 생긴 실패를 구분한다.
- 실행하지 않은 빌드, 에뮬레이터 확인 또는 자산 검증을 성공했다고 보고하지 않는다.

## 완료 보고

다음을 보고한다.

- 변경한 파일
- 구현한 동작 또는 확정한 규칙
- 실행한 검증 명령과 결과
- 실행하지 못한 검증과 이유
- 남은 위험과 수동 에뮬레이터 확인 항목
